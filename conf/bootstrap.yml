server :
  port : 8761

spring:
  application:
    name: cam-service-registry


eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    evication-interval-timer-in-ms: 10000